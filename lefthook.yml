pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{ts,tsx,js,mjs,json}"
      run: pnpm check:staged-lint {staged_files}

pre-push:
  parallel: true
  commands:
    typecheck:
      run: pnpm check:types
    test:
      run: pnpm check:unit
