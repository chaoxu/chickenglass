pre-commit:
  commands:
    pnpm-version:
      run: pnpm check:pnpm-version
    lint:
      glob: "*.{ts,tsx,js,mjs,json}"
      run: pnpm check:staged-lint {staged_files}

pre-push:
  commands:
    pnpm-version:
      run: pnpm check:pnpm-version
    fast-check:
      run: pnpm check:pre-push
