/**
 * @deprecated Import from `../lib/context-menu` instead.
 *
 * Re-exports from the canonical location so existing app/ imports continue
 * to work without changes.
 */
export type { ContextMenuItem } from "../lib/context-menu";
export { ContextMenu } from "../lib/context-menu";
