/**
 * @deprecated Import from `../../lib/project-paths` instead.
 *
 * Re-exports from the canonical location so existing app/ imports continue
 * to work without changes.
 */
export {
  normalizeProjectPath,
  resolveProjectPathFromDocument,
  projectPathCandidatesFromDocument,
  relativeProjectPathFromDocument,
} from "../../lib/project-paths";
